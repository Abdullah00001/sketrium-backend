version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: sketrium_backend_redis
    ports:
      - "6379:6379"
    volumes:
      - sketrium_redis_data:/data
    restart: always

volumes:
  sketrium_redis_data:
